<Window x:Class="Mestr.UI.View.EconomyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Transaction"
        Width="400"
        Background="#F9FAFB"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        SizeToContent="Height"
        MaxHeight="700">

    <Window.Resources>
        <!-- Animated Error Template -->
        <ControlTemplate x:Key="AnimatedErrorTemplate">
            <Border BorderBrush="Red" BorderThickness="2" CornerRadius="3" Opacity="0">
                <AdornedElementPlaceholder/>
                <Border.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="0" To="1" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>
            </Border>
        </ControlTemplate>

        <!-- TextBox Style -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource AnimatedErrorTemplate}"/>
            <Setter Property="ToolTip"
                    Value="{Binding RelativeSource={RelativeSource Self},
                                    Path=(Validation.Errors)[0].ErrorContent}"/>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource AnimatedErrorTemplate}"/>
            <Setter Property="ToolTip"
                    Value="{Binding RelativeSource={RelativeSource Self},
                                    Path=(Validation.Errors)[0].ErrorContent}"/>
        </Style>

        <!-- DatePicker Style -->
        <Style x:Key="ModernDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource AnimatedErrorTemplate}"/>
            <Setter Property="ToolTip"
                    Value="{Binding RelativeSource={RelativeSource Self},
                                    Path=(Validation.Errors)[0].ErrorContent}"/>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="Tilføj post" FontSize="20" FontWeight="Bold" Foreground="#333" Margin="0 0 0 15"/>

            <!-- Transaction Type -->
            <Grid Grid.Row="1" Margin="0 4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Transaktionstype" FontSize="11" Foreground="#555" Margin="0 0 0 2"/>
                <ComboBox Grid.Row="0"
                          ItemsSource="{Binding TransactionTypes}"
                          SelectedItem="{Binding SelectedTransactionType}"
                          Style="{StaticResource ModernComboBoxStyle}"
                          Height="30"/>
            </Grid>

            <!-- Name -->
            <Grid Grid.Row="2" Margin="0 4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Navn" FontSize="11" Foreground="#555" Margin="0 0 0 2"/>
                <TextBox x:Name="nameBox"
                         Grid.Row="0"
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Height="30"/>
                <ItemsControl Grid.Row="1"
                              ItemsSource="{Binding Path=(Validation.Errors), ElementName=nameBox}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Foreground="Red" FontSize="11" Text="{Binding ErrorContent}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>

            <!-- Description -->
            <Grid Grid.Row="3" Margin="0 4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Beskrivelse" FontSize="11" Foreground="#555" Margin="0 0 0 2"/>
                <TextBox Grid.Row="0"
                         Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Height="30"/>
            </Grid>

            <!-- Amount -->
            <Grid Grid.Row="4" Margin="0 4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Beløb" FontSize="11" Foreground="#555" Margin="0 0 0 2"/>
                <TextBox x:Name="amountBox"
                         Grid.Row="0"
                         Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                         PreviewTextInput="AmountBox_PreviewTextInput"
                         DataObject.Pasting="AmountBox_Pasting"
                         TextChanged="AmountBox_TextChanged"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Height="30"/>
                <ItemsControl Grid.Row="1"
                              ItemsSource="{Binding Path=(Validation.Errors), ElementName=amountBox}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Foreground="Red" FontSize="11" Text="{Binding ErrorContent}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>

            <!-- Category -->
            <Grid Grid.Row="5" Margin="0 4" Visibility="{Binding CategoryVisibility}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Kategori" FontSize="11" Foreground="#555" Margin="0 0 0 2"/>
                <ComboBox Grid.Row="0"
                          ItemsSource="{Binding Categories}"
                          SelectedItem="{Binding SelectedCategory}"
                          Style="{StaticResource ModernComboBoxStyle}"
                          Height="30"/>
            </Grid>

            <!-- Date -->
            <Grid Grid.Row="6" Margin="0 4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Dato" FontSize="11" Foreground="#555" Margin="0 0 0 2"/>
                <DatePicker Grid.Row="0"
                            SelectedDate="{Binding Date, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                            Style="{StaticResource ModernDatePickerStyle}"
                            Height="40"/>
            </Grid>

            <!-- Buttons -->
            <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 15 0 0">
                <Button Content="Annuller" Width="80" Margin="5"
                        Command="{Binding CancelCommand}"
                        Background="#E5E7EB" Foreground="#333"/>
                <Button Content="Gem" Width="80" Margin="5"
                        Command="{Binding SaveCommand}"
                        Background="#2563EB" Foreground="White"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>