<UserControl x:Class="Mestr.UI.View.ProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Language="da-DK">

    <UserControl.Resources>
        <!-- Animated Error Template -->
        <ControlTemplate x:Key="AnimatedErrorTemplate">
            <Border BorderBrush="Red" BorderThickness="2" CornerRadius="3" Opacity="0">
                <AdornedElementPlaceholder/>
                <Border.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="0" To="1" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>
            </Border>
        </ControlTemplate>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0057E8"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Disabled State -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#B8D4F7"/>
                                <Setter Property="Foreground" Value="#94A3B8"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <!-- Hover State -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0046BA"/>
                            </Trigger>
                            <!-- Pressed State -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#003A9B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource AnimatedErrorTemplate}"/>
            <Setter Property="ToolTip"
                    Value="{Binding RelativeSource={RelativeSource Self},
                                    Path=(Validation.Errors)[0].ErrorContent}"/>
        </Style>

        <!-- DatePicker Style -->
        <Style x:Key="ModernDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Validation.ErrorTemplate" Value="{StaticResource AnimatedErrorTemplate}"/>
            <Setter Property="ToolTip"
                    Value="{Binding RelativeSource={RelativeSource Self},
                                    Path=(Validation.Errors)[0].ErrorContent}"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#FFF5F7FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="10">
            <Button Content="Home" Command="{Binding NavigateToDashboardCommand}" DockPanel.Dock="Left"
                    FontSize="12" Height="34" Width="71"
                    Background="White" Foreground="Black"
                    BorderThickness="1" Padding="14,8"
                    FontWeight="Bold" Cursor="Hand" BorderBrush="Black">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
            <TextBlock Text="Nyt projekt" DockPanel.Dock="Top"
                       FontSize="20" FontWeight="SemiBold"
                       HorizontalAlignment="Center" Foreground="#1E293B"/>
        </DockPanel>

        <!-- Main Content -->
        <Border Grid.Row="1" Background="White" Margin="20" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="270"
                                  ShadowDepth="5" Opacity="0.25" BlurRadius="20"/>
            </Border.Effect>

            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="1*" MaxWidth="400" MinWidth="250"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Column -->
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Oprettelse af nyt projekt"
                               FontSize="16" Margin="0,0,0,20"
                               Foreground="#1E293B" FontWeight="SemiBold"/>

                    <!-- Project Name -->
                    <TextBlock Text="PROJEKTNAVN" Foreground="#475569" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <Grid Margin="0 0 0 15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <!-- Input -->
                            <RowDefinition Height="20"/>
                            <!-- Fixed space for error -->
                        </Grid.RowDefinitions>
                        <TextBox x:Name="NameBox"
                                 Grid.Row="0"
                                 Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                                 Style="{StaticResource ModernTextBoxStyle}"
                                 Height="30"/>
                        <ItemsControl Grid.Row="1"
                                      ItemsSource="{Binding Path=(Validation.Errors), ElementName=NameBox}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Foreground="Red" FontSize="11" Text="{Binding ErrorContent}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>

                    <!-- Client Name -->
                    <TextBlock Text="KUNDENAVN" Foreground="#475569" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <Grid Margin="0 0 0 15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                        </Grid.RowDefinitions>
                        <TextBox x:Name="ClientBox"
                                 Grid.Row="0"
                                 Text="{Binding ClientName, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                                 Style="{StaticResource ModernTextBoxStyle}"
                                 Height="30"/>
                        <ItemsControl Grid.Row="1"
                                      ItemsSource="{Binding Path=(Validation.Errors), ElementName=ClientBox}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Foreground="Red" FontSize="11" Text="{Binding ErrorContent}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>

                    <!-- Deadline -->
                    <TextBlock Text="DEADLINE" Foreground="#475569" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <Grid Margin="0 0 0 15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                        </Grid.RowDefinitions>
                        <DatePicker x:Name="DeadlineBox"
                                    Grid.Row="0"
                                    SelectedDate="{Binding Deadline, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                                    Style="{StaticResource ModernDatePickerStyle}"
                                    Height="40"
                                    Language="da-DK"/>
                        <ItemsControl Grid.Row="1"
                                      ItemsSource="{Binding Path=(Validation.Errors), ElementName=DeadlineBox}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Foreground="Red" FontSize="11" Text="{Binding ErrorContent}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </StackPanel>

                <!-- Right Column -->
                <Grid Grid.Column="2" Margin="20,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="1" Text="BESKRIVELSE" Foreground="#475569" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Grid.Row="2"
                             Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                             TextWrapping="Wrap"
                             HorizontalAlignment="Stretch"
                             VerticalAlignment="Stretch"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Annuller" 
                    Command="{Binding NavigateToDashboardCommand}" 
                    Margin="0,0,10,0"
                    Style="{StaticResource ModernButtonStyle}"/>
            <Button Content="Opret projekt" 
                    Command="{Binding CreateProjectCommand}"
                    Style="{StaticResource ModernButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>