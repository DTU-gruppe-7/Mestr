<UserControl x:Class="Mestr.UI.View.ProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mestr.UI.View"
             xmlns:viewmodels="clr-namespace:Mestr.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <Grid Background="#FFF5F7FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="10">
            <Button Content="Home" DockPanel.Dock="Left"
                   
                    FontSize="12" Height="34" Width="71"
                    Background="White" Foreground="Black"
                    BorderThickness="1,1,1,1" Padding="14,8"
                    FontWeight="Bold" Cursor="Hand" BorderBrush="Black">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
            <TextBlock Text="Nyt projekt" DockPanel.Dock="Top"
                       FontSize="20" FontWeight="SemiBold"
                       HorizontalAlignment="Center" Foreground="#1E293B"/>
        </DockPanel>

        <Border Grid.Row="1" Background="White" Margin="20" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="270"
                                  ShadowDepth="5" Opacity="0.25" BlurRadius="20"/>
            </Border.Effect>

            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="1*" MaxWidth="400" MinWidth="250"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Oprettelse af nyt projekt"
                               FontSize="16" Margin="0,0,0,20"
                               Foreground="#1E293B" FontWeight="SemiBold"/>

                    <TextBlock Text="PROJEKTNAVN" 
                               Foreground="#475569" 
                               FontWeight="SemiBold" 
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,15" 
                             HorizontalAlignment="Stretch"/>

                    <TextBlock Text="KUNDENAVN" 
                               Foreground="#475569" 
                               FontWeight="SemiBold" 
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding ClientName, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,15" 
                             HorizontalAlignment="Stretch" />

                    <TextBlock Text="DEADLINE" 
                               Foreground="#475569" 
                               FontWeight="SemiBold" 
                               Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding Deadline}"
                                HorizontalAlignment="Stretch" 
                                Margin="0,0,0,15" />
                </StackPanel>

                <Grid Grid.Column="2" Margin="20,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text=" " 
                               FontSize="16" Margin="0,0,0,20" />

                    <TextBlock Grid.Row="1" Text="BESKRIVELSE" 
                               Foreground="#475569" 
                               FontWeight="SemiBold" 
                               Margin="0,0,0,5"/>

                    <TextBox Grid.Row="2"
                             Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                             TextWrapping="Wrap" 
                             HorizontalAlignment="Stretch" 
                             VerticalAlignment="Stretch" AcceptsReturn="True" 
                             VerticalScrollBarVisibility="Auto"/>
                </Grid>

            </Grid>
        </Border>
        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="10">
            <Button Content="Annuller" Command="{Binding CancelCommand}" Margin="0,0,10,0"
                    
                    Background="#0057E8" Foreground="White"
                    BorderThickness="0" Padding="14,8"
                    FontWeight="SemiBold" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Content="Opret projekt" Command="{Binding CreateProjectCommand}"
                    
                    Background="#0057E8" Foreground="White"
                    BorderThickness="0" Padding="14,8"
                    FontWeight="SemiBold" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
